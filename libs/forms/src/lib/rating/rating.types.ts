/**
 * Public types for Rating. Re-exports the variant types so consumers can use
 * them in their own component inputs.
 */
export type { RatingVariant, RatingSize, RatingVariants } from './rating.variants';
