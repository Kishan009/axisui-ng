{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "AxisUiNgAdd",
  "title": "AxisUI ng-add options",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The workspace project to set up. Defaults to the default/first project.",
      "$default": { "$source": "projectName" }
    }
  },
  "additionalProperties": false
}
