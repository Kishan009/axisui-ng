/**
 * Public types for the Button component. Re-exports the variant types
 * so consumers can use them in their own component inputs.
 */

export type { ButtonVariant, ButtonSize, ButtonVariants } from './button.variants';
