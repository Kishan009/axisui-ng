{
  "name": "overlays",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/overlays/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:lib", "role:category", "license:mit"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": { "project": "libs/overlays/ng-package.json", "tsConfig": "libs/overlays/tsconfig.lib.json" }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "jestConfig": "libs/overlays/jest.config.js" }
    },
    "lint": { "executor": "@nx/eslint:lint", "outputs": ["{options.outputFile}"] }
  }
}
